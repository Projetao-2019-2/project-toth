components:
  schemas:
    BasicNotificationsModel:
      properties:
        id:
          type: integer
        texto:
          type: string
        link:
          type: string
        visualizado:
          type: boolean
        userid:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

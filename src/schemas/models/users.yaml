components:
  schemas:
    BasicUsersModel:
      properties:
        id:
          type: integer
        nome:
          type: string
        email:
          type: string
          format: email
        curso:
          type: string
        ies:
          type: string
        senha:
          type: string
          format: password
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
